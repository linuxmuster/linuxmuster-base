# stop all relevant services

services="linuxmuster-base atftpd rsync linbo-multicast nagios2 atd cron\
          tpmfos rbagent dbgw tpmfos-derby \
          webmin rembo quota opengroupware.org apache2 cupsys \
          clamav-daemon cyrus2.2 postfix saslauthd amavis mysql ntp-server \
          ssh samba slapd postgresql nscd linuxmuster-ipcop uml-utilities \
          portmap inetd bind9 dhcp3-server"

for service in $services; do

	[ -e "/etc/init.d/$service" ] && /etc/init.d/$service stop

done

if [ "$1" = "--first" ]; then

  # reconfigure locales
  DEBIAN_FRONTEND=noninteractive dpkg-reconfigure locales

fi
